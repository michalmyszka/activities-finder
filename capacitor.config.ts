import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.michalmyszka.activities-starter',
  appName: 'Activities Finder',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config
